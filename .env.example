# Copy this file to `.env` and fill in your Mapbox public access token.
# Get one at https://account.mapbox.com/access-tokens/
# The token is used only for downloading satellite tiles via the Static Images API.
MAPBOX_TOKEN=pk.your_public_token_here