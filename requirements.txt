# Production runtime dependencies — ONLY what the deployed API actually imports.
#
# Resolved transitively from the inference request path:
#   app.backend.main -> src.inference.{predict,artifacts,explain}
#                    -> src.features.build_features -> src.config, src.utils
#                    -> XGBoost native TreeSHAP (pred_contribs=True; no shap package)
#
# Research / training deps (torch, vision, notebooks, plotting, extra booster
# libs, data acquisition, shap for research plots) live in
# requirements-research.txt and are NOT deployed.

numpy>=1.24
pandas>=2.0
scipy>=1.10

scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3

# API runtime
fastapi>=0.110
uvicorn>=0.28
pydantic>=2.5

# Testing (CI) — kept here so `pip install -r requirements.txt` supplies pytest
pytest>=7.0
httpx>=0.27